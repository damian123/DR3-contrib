# Portfolio stress example

The deterministic self-test prices 17 spot shocks and verifies that call
prices remain finite and strictly increasing. It does not execute timing
loops. A normal invocation prints a local measurement whose result depends on
CPU, compiler, ISA, and build type; no fixed speedup is claimed.
